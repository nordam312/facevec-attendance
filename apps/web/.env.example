# Web dashboard — environment template.
# NEXT_PUBLIC_* values are inlined into the client bundle at build time.

# Base URL the browser uses to reach the API gateway.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
